name: RPA bisect call11

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing stable tag to backfill, for example v0.7.6
        required: true
        type: string

permissions:
  contents: read

jobs:
  call-v5:
    name: Call pd-v5
    uses: ./.github/workflows/pd-v5.yml
    with:
      tag: v0.8.0
      dry_run: true
