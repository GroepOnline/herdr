name: RPA bisect call10

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing stable tag to backfill, for example v0.7.6
        required: true
        type: string

permissions:
  contents: read

jobs:
  call-v4:
    name: Call pd-v4
    uses: ./.github/workflows/pd-v4.yml
    with:
      tag: v0.8.0
      dry_run: true
