name: RPA bisect call6

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing stable tag to backfill, for example v0.7.6
        required: true
        type: string

permissions:
  contents: read

jobs:
  call-both:
    name: Call both-trigger reusable
    uses: ./.github/workflows/pd-both.yml
    with:
      tag: v0.8.0
